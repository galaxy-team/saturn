#include <vector>

namespace galaxy {
    namespace saturn {
        namespace utilities {
//            bool is_read_only(std::string filename);
            std::vector<std::uint16_t> string_to_vector(char * string);
        }
    }
}
